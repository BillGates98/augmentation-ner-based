
from scraper import Scraper

class Mapper: 

    def __init__(self, literal='', regexps=[]):
        super().__init__()
        self.literal = literal
        self.regexps = regexps

    def extract(self):
        # Scraper().run()
        return ''
    
    def run(self):
        return ''
